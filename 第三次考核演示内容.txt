1、SRC与putty的区别
	分组加密的几种方式
	ECB：是一种基础的加密方式，密文被分割成分组长度相等的块（不足补齐），然后单独一个个加密，一个个输出组成密文。
	CBC：是一种循环模式，前一个分组的密文和当前分组的明文异或操作后再加密，这样做的目的是增强破解难度。
	CFB/OFB实际上是一种反馈模式，目的也是增强破解的难度。

2、ssh_config详解

3、回收站
	root的.bashrc文件
	所有用户适用的profile文件。。/etc/pro*

4、linux用户权限
	1.passwd权限
	2.group权限

5、linux文件权限
	1.rwx权限（000-777）
	2.特殊的chroot
	3.acl

6、项目/网站效率查询
	testStudio和GTmetrix

7、效率之mysql

8、开机自启动
	1.直接设置/etc/rc.d/rc.local文件
	2.添加/etc/rc.d/init.d/文件，chkconfig设置
	3.添加/etc/rc.d/rc.sysinit启动命令

9、memcached
	1.telnet连接本地
	2.php开启memcached


10、忽略上传
	1.直接在/.git/info/exclude里面添加需要忽略的文件
	2.创建一个.gitignore文件

11、git stash用户工作区暂存
	命令分别为git stash
		  git stash apply
	git stash pop
	git stash list
	git stash clear

12、git权限管理之——gitolite！



nginx安装和支持PHP











